{% set type = type is defined ? type : 'link' %}
{% set bgImag = bgImag is defined ? bgImag : 0 %}
{% set linkTarget = extern is defined and extern ? '_blank' : '_self' %}

{% set backgrounds = ['@webroot/assets/img/button-1.svg','@webroot/assets/img/button-2.svg','@webroot/assets/img/button-3.svg'] %}

{% if type == 'link' %}
    <a href="{{ target }}" target="{{ linkTarget }}" class="relative block text-center font-black text-2xl text-yellow hover:text-black group px-4 py-5">
        {{ svg(backgrounds[bgImag])|attr({ class: 'absolute inset-1 -z-10 block h-full w-full fill-current' }) }}
        <span class="inline-block  align-bottom pt-2 text-black group-hover:text-yellow leading-[35px]">
             {{ label }}
        </span>
    </a>
{% endif %}